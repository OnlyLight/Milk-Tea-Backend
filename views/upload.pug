<!DOCTYPE html>
html(lang="en")
head
	meta(charset="UTF-8")
	title Document
form(action='/upload', method='POST', enctype='multipart/form-data')
	label(for='ht') Choose Your file: 
	input#ht(type='file', name='file')
	br
	input(type='submit', value='Submit')

	